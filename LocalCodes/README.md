# Local Codes

Place private App Store Connect offer-code CSV files and optional local `products.json` seed data in this folder for local Debug builds.

CSV and JSON files in this folder are ignored by git and copied into the debug app bundle by the Xcode build script. They are not copied for Release builds.
